const LIMIT = 10;

enum HTTP_METHOD {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

enum USER_STATUS {
  ACTIVE,
  BLOCK,
}

enum ORDER_STATUS {
  PENDDING,
  APPROVE,
  CANCLE,
}

export { LIMIT, HTTP_METHOD, USER_STATUS, ORDER_STATUS };
